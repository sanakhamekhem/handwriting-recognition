function [ observations ] = letterObservations( letterImg, models, handleDisplayAt )
    %This is a supervisor function to call different HMM observation extraction
    %methods
    %Get number of models
    nmodels = size(models,1);
    for i = 1 : nmodels
        
    end
end

